# Handoff — 2026-09-13

For any session picking up the hunter after this one. Everything below is on
`main` and in the shared Supabase project; nothing lives only in a chat.

## Read these first, in this order

1. **`prompts/enumeration.md`** — the agent template. Two sections are new and
   binding: *Owner scope rules — set 2026-09-13* (churches out, pastors out
   unless non-church nonprofit, Christian/Catholic/non-religious only, US+Canada
   in scope, and the owner keyword-exclusion list) and the corrected
   search-operator guidance (rule 3 and rule 5).
2. **`SEARCH-OPERATORS.md`** — `blocked_domains` works; bare `-term` is
   best-effort; `-"phrase"` and `-site:` do not work; the tool's prose summary
   is not evidence.
3. **`ORG-TRIAGE-PLAN.md`** — §3 has the owner's answers, §4 is the ingest gate
   still to build, §6 is what execution taught (a per-row trigger on
   `hunt_targets`, and server-side `statement_timeout`).
4. **`WORKED.md`** and **`AUDIT-2026-09-12.md`** — how to check holdings before
   dispatching (org name + FK, never a hand-typed domain).

## What changed in the database today

- **Org queue emptied**: 1,263 pending org rows → 501 re-typed as people, 471
  rejected with a reason + `hunt_negatives` entry, 13 retired as duplicates,
  **283 promoted to `hunt_targets` as `unrostered`** (`discovered_by =
  'org-triage:2026-09-13'`; 46 carry a support-raised signal, `tier_profile='A'`).
- **395 re-typed people** are `pending`, linked to a target for most, and
  **only 61 are evidenced** — the July import recorded *how* it found each
  person (`meta.source` = `website` / `search` / `enrichment`) but not *where*.
  A citation check on 2026-09-13 set `meta.citation_quality` on every row and
  scored the uncited ones 5: 61 `per_person_page` (score kept), 32
  `agency_homepage`, 3 `crowdfund_platform`, 31 `email_only`, 63
  `linkedin_only`, 153 `none`. **Approve the 61; the other 282 need a citation
  before they are approvable.** `meta.retype_bucket in ('0a','4')` finds them.
- **ISI**: all 196 approved with `meta.role_status`. Agency `exhausted` at 186.
- **837 tenure/age-flagged people** annotated `meta.tenure_age_review`
  (owner-cleared; not a contact blocker).
- **Keyword exclusion applied**: 4 targets + 1 org row rejected, 6 negatives.
  **Org rows only. Never a person.** A person named Gay was correctly left alone.
- **Faith rule applied**: 13 LDS person rows and 4 interfaith orgs rejected;
  2 in review. Catholic and Messianic are Christian for this purpose.
- InterVarsity reopened (negative 63 → `reversed`); MAF duplicate target #27
  retired; Gospel for Asia rejected `pooled_fund`.

Reversal tables, all dated: `org_triage_20260913_class` (the master map, holds
prior org_type/status/meta for all 1,263), `_step3`, `_promote`,
`keyword_exclusion_20260913`, `faith_rule_20260913`, `isi_owner_approve_20260912`,
`tenure_age_review_20260912`, `scout_approve_20260912`, `isi_role_fix_20260912`.

## Still open

- **Decision 5**: run the giving-domain screening wave on the 283 promoted
  targets (~283 searches, one per org, `blocked_domains: ["en.wikipedia.org"]`
  on every query), or hand-pick from the 46 with a signal first.
- **Settled 2026-09-14 — no longer open.** The owner approved all 395
  re-typed people, the 282 uncited ones included, at fit_score 5. Each still
  carries `meta.citation_quality` recording why it was scored down
  (32 agency_homepage, 3 crowdfund, 31 email_only, 63 linkedin_only,
  153 none), so a later citation pass can lift them without re-deriving it.
  Bearing on the email work: those 282 are precisely the people with no
  per-person page, so extraction cannot produce an address for them. The 63
  with a LinkedIn profile and the 31 with an email already on the record are
  the tractable slice.
- **The ingest gate** (plan §4 A–E): people-only trigger on `scout_candidates`,
  route the legacy import path through `hunt_targets`, name-shape tripwire with
  the bias fix, constrained `org_type` vocabulary, keyword screen at ingest.
- **Rewrite `hunt_target_link_candidates()` as a statement-level trigger** —
  the per-row version makes any bulk insert into `hunt_targets` time out.
- InterVarsity (#496) — giving-domain test before any wave.
- Two review rows: Tanenbaum Center, Building Bridges Together (faith orientation).
- Long tail unchanged: 12 unattributed pending people; person-shaped rows in
  `sales.leads`; ~60–80 non-ICP people rows; 27 unmapped legacy singletons.

## RETRACTED 2026-09-22 — the email section below is wrong

The section that follows claimed 5,705 people had an unread published email
behind a stored page, and recommended FOCUS as a 1,188-address first pass.
**Both claims are false.** `tools/hunter/harvest-contacts.mjs` in the
`sac45310-Dev/donor-send` repository already harvested the entire rostered
population on 2026-09-21. FOCUS yielded 568 addresses and 1,196 phone numbers;
the FOCUS people still without an email do not publish one. Baptist Bible
Fellowship, Baptist Missions to Forgotten Peoples, the three SIM arms,
Wycliffe, Ethnos360, RUF and ISI were all measured at or near zero.

`tools/hunter/enrich/` is a worse reimplementation of that tool and must not
be run. See its README for what it lacks.

The live contact pipeline lives in `donor-send`, not here. This repository's
`tools/hunter/` is documentation and analysis; the executable hunter is the
`/hunt` skill in that other repository.

## Emails: the next big lift is extraction, not hunting  (added 2026-09-21, RETRACTED — see above)

8,092 approved people are live as leads and 6,191 have no email, but 5,705 of
those already have a per-person URL stored. The address is on a page we hold
and has never been read off it.

Root cause: `public.sales_enrich_claim` selects `where sc.status = 'pending'`,
so the approved population has always been invisible to the enrichment queue.

Tooling is in `tools/hunter/enrich/` (worklist SQL, two-phase extractor,
17 passing tests, README with the per-agency coverage table). The parse phase
runs anywhere; the fetch phase needs egress to the agency domain, which the
web session does not have.

Best first pass: FOCUS, 1,188 people with a `focus.org/missionaries/` page and
no email, on a domain that already yielded 570 clean addresses.

Caveats carried in the README: no email in the CRM is verified, 300 are
pattern-inferred rather than published, and the contact-to-candidate key is
populated on only 2,327 of 7,302 contact rows.

## Rules that must survive any rewrite of the agent

- Organisations are approached first. Individuals are contacted only once
  their organisation has given permission (owner decision, 2026-09-21).
  Personal emails are in scope: they uniquely identify each person inside an
  organisation and are what that permissioned outreach runs on.
- Agents never record or infer any individual's demographic or identity
  attributes. The keyword rule is org-level only.
- `faith_orientation` and `do_not_pursue` are owner-set; agents flag, never classify.
- Initials-only, first-name-only and codename records are never emitted and
  never resolved from another source.
- A hold by any agent binds every agent; never resolve by majority.
- Per-agent yield is not wave yield; recompute against the CRM after merge.
- A tool's prose summary is not a tool result; read the URL list.
- Negatives are amended (`reversed`), never deleted.
